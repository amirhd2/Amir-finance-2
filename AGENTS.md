# Project Rules & Conventions

## قوانین انیمیشن
- **قانون انیمیشن‌های باز و بسته شدن**: هرگاه انیمیشنی برای هر قسمت، کارت، مودال یا المانی در برنامه در نظر گرفته می‌شود، حتماً باید هم انیمیشن باز شدن (ورود / mount) و هم انیمیشن بسته شدن (خروج / unmount) داشته باشد (با استفاده از `AnimatePresence` و انیمیشن‌های روان `opacity`, `height`, `scale`, یا `y`).

## یکپارچگی پنجره‌های کارت استک و استیکی کارت‌ها
- **قانون ظاهر یونیورسال پنجره‌های استک/استیکی**: فرقی نمی‌کند که پنجره کارت‌های استک و استیکی کارت‌ها از چه طریقی باز شود (منوی شناور FAB، پروفایل مخاطب، صفحه وام، دکمه‌های صفحه اصلی یا هر جای دیگر)، ظاهر پس‌زمینه، استاتوس بار، سربرگ و کنترل‌های آن باید کاملاً یکدست و یونیورسال باشند و منوی شناور نباید استایل جداگانه داشته باشد.

## قفل بودن قطعی دارایی‌های تصویری (LOCKED IMAGE ASSETS)
- **ممنوعیت مطلق تغییر آیکون‌ها و تصاویر**: تمامی فایل‌های آپلود شده شامل Favicon ها، Apple Touch Icon، آیکون‌های اندروید و PWA (192x192 و 512x512 و Maskable)، اسپلش اسکرین‌ها و تمام فایل‌های تصویری دستی، **کاملاً قفل شده و غیرقابل تغییر (IMMUTABLE)** هستند.
- هوش مصنوعی تحت هیچ شرایطی اجازه ندارد این فایل‌ها را دوباره تولید کند، نام آن‌ها را عوض کند، تغییر سایز یا فرمت دهد، یا در پوشه دیگری ذخیره کند.
- مسیر این فایل‌ها در `index.html`، `manifest.webmanifest` و تنظیمات برنامه قفل است. در زمان توسعه UI، تم، کدهای طراحی یا عملیات بیلد، نباید هیچ تغییری در فراخوانی، بهینه‌سازی یا مسیر این تصاویر ایجاد شود.

## قانون قفل نام‌گذاری ۳ خطی تراکنش‌ها (LOCKED TRANSACTION NAMING SYSTEM)
در تمامی بخش‌های برنامه (داشبورد، لیست همه تراکنش‌ها، بدهی‌ها، طلب‌ها، وام‌ها و پرونده‌های بایگانی)، ساختار نمایش کارت‌های تراکنش در `SwipeableTxCard` باید دقیقا و بدون استثنا به شکل ۳ خطی زیر رعایت و قفل شود:
- **بدهی جدید**:
  - خط اول: `"بدهی جدید ثبت شد"`
  - خط دوم: `نام مخاطب` (مثال: `"نرگس محمدی"`)
  - خط سوم: `متن توضیحات` (در صورت نبود توضیحات: `"توضیحات ثبت نشده"`)
- **بازپرداخت بدهی**:
  - خط اول: `"بازپرداخت جدید ثبت شد"`
  - خط دوم: `نام مخاطب` (مثال: `"نرگس محمدی"`)
  - خط سوم: `متن توضیحات`
- **طلب جدید**:
  - خط اول: `"طلب جدید ثبت شد"`
  - خط دوم: `نام مخاطب` (مثال: `"نرگس محمدی"`)
  - خط سوم: `متن توضیحات`
- **بازپرداخت طلب**:
  - خط اول: `"بازپرداخت جدید ثبت شد"`
  - خط دوم: `نام مخاطب` (مثال: `"نرگس محمدی"`)
  - خط سوم: `متن توضیحات`
- **قسط وام**:
  - خط اول: `"پرداخت قسط [شماره] وام [نام وام]"` (بدون کلمه «شماره»، مثال: `"پرداخت قسط 12 وام ایران خودرو"`)
  - خط دوم: `نام مخاطب / طرف حساب`
  - خط سوم: `متن توضیحات`

